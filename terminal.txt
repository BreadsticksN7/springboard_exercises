mkdir first
cd first
touch person.txt
mv person.txt another.txt
cp another.txt copy.txt
cd..
cp -r first second
rm -rf second


enter / q
-a Include directory entries whose names begin with a dot (.).
-l List in long format.  (See
             below.)  A total sum for all the file sizes is output on a line
             before the long listing.

option left / right
control e
option mouse click
control w
Terminal is a program with a CLI / shell is what defines the output (ubuntu for example)
Absolute path is the full path to the location
Relative path is the path from the location you preside in
Flags are attributes to commands to give them certain tasks
    rm -rf (attempts to remove file heirarcy and without confirmation per file)
    ls -l (lists files in sub with additional details)

